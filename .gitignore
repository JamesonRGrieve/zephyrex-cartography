node_modules/
dist/
coverage/
.coverage/
storybook-static/
*.log
.DS_Store
.foundry-test-data-*/
# A local Foundry v14 release (or a symlink to one) for the e2e suite; never committed.
.foundry-release
# Optional third-party modules for the e2e suite (Item Piles and its dependencies); never committed.
.foundry-test-modules
test-results/
.e2e-results.json
.e2e-raw-coverage/
.e2e-coverage/

# Ratchet run reports — regenerated on every gate run; the committed source of
# truth is the paired `.*-baseline` file, not these detail dumps.
.ts-coverage.json
.strict-coverage.json
.test-typecheck-coverage.json
.husky/_logs/
